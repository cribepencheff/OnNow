Typed clients for external data sources (TVmaze), called only from query hooks.

`fixtures/` holds real TVmaze responses for the test set in
`docs/spikes/README.md`, used by this folder's tests. Refresh them with
`npm run fixtures:update`.
